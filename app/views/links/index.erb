<h1> You're filtered tags, sir!</h1>

<ul id='links'>
  <% @links.each do |link| %>
  <li>
    Title <%= link.title %>
    URL:  <%= link.url %>
    Tag: <%= link.tags.map(&:tag).join(', ') %>
  </li>
  <% end %>
</ul>
